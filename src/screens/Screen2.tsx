import React from 'react';
import {Text, View} from 'react-native';

interface IProps {}

const Screen2 = ({}: IProps) => {
  return (
    <View style={{}}>
      <Text>Screen2</Text>
    </View>
  );
};

export default Screen2;
